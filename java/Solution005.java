/*
Dado un string text y un string prefix, devuelve true si text comienza exactamente
 con prefix, o false en caso contrario.

La comparación es sensible a mayúsculas y minúsculas.
*/
public class Solution005 {
    public boolean startsWith(String text, String prefix) {
        return text.startsWith(prefix);
    }
}
